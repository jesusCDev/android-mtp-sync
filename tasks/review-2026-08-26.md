# Validation review — 2026-08-26

Four reviewers (core transfer logic, web UI/security, CLI/config, tests/docs) + inline verification.
Baseline: `pytest tests/` = 3 failed / 11 passed. `py_compile` OK. Uncommitted diff (palette rewrite) breaks runner.py.

## P0 — data loss / silent no-op / security

| # | Where | Problem | Fix |
|---|-------|---------|-----|
| 1 | runner.py:101 | `gio_utils.DRY_RUN = True` never reset. In the long-lived web server, the first dry run (dashboard default) poisons the global — every later "execute" run is a silent no-op reported as success. | `gio_utils.DRY_RUN = dry_run` unconditionally, or pass it down. |
| 2 | operations.py:234,254,270,280,283,292,302 | Backup writes real resume state in dry-run (`gio_copy` returns True under DRY_RUN). `--run` (dry) then `--run -y` copies zero files, prints "Already in sync". `mark_rule_complete` wipes state. | Gate every `state.*` write on `not gio_utils.DRY_RUN`. |
| 3 | operations.py:500 | Move deletes phone original after verifying only `dest.exists() and st_size > 0`. Truncated MTP copy passes; source destroyed. 0-byte files never verify and are re-copied as `name (1)` every run. | Compare dest size to source size (already in hand at :487). |
| 4 | operations.py:570,587,613 | Sync: desktop path is a file / empty mountpoint → `expected_phone_files` empty → `delete_extraneous` (default **True** via `.get`) deletes every file in the phone target. | Require `is_dir()`; refuse delete when source scan yields 0 files; runtime default False. |
| 5 | operations.py:298 | `final_copied >= total - failed` is true when everything fails (0 >= 0). Phone disconnects mid-backup → "All files copied", state wiped. | `failed == 0 and copied + skipped >= total`. |
| 6 | operations.py:92,242,338,438,527,673 / :621,632 | Entry names from `gio_list` appended raw onto percent-encoded URIs. `a b#c.jpg` truncates at `#`; `100%.jpg` errors. Files silently skipped, run reports success. In sync, mis-named file then looks extraneous → deleted. | `urllib.parse.quote(entry, safe='')` on every appended segment. |
| 7 | gio_utils.py:144 | `gio_list` returns `[]` on non-zero returncode. Transient MTP failure = "empty dir" → move tries to delete dir; backup declares complete with 0 files. | Propagate failure (raise / return None). |
| 8 | gio_utils.py:175 | `--backup=none` is not a valid gio value; glib parses it as boolean `-b`. Every sync overwrite leaves `filename~` on phone. `gio copy` already overwrites by default. | Drop the flag and the `overwrite` param. |
| 9 | state.py:28 | Corrupt/truncated state.json swallowed into `{}`; next save writes back only the current rule — every other rule's resume progress destroyed. | Rename to `state.json.corrupt`, warn. |
| 10 | web_ui.py:19 | `CORS(app)` = `Access-Control-Allow-Origin: *`, no auth, no CSRF, no Origin check. Any web page you visit can list your filesystem (`/api/browse/desktop?path=~/.ssh`), create rules, and POST `/api/run {dry_run:false}` to delete files off the phone. Localhost bind does not help. | Delete `CORS(app)`; add Origin/`Sec-Fetch-Site` check or token on mutating routes. |
| 11 | web_ui.py:494,521 | Desktop browse/mkdir take any absolute path — no confinement to `$HOME`. | `realpath` + `is_relative_to(Path.home())`, reject names with `/`. |
| 12 | web_ui.py:442 / paths.py | `normalize_phone_path` keeps `..` segments → `?path=/../../` escapes storage prefix. | Drop `.`/`..` segments in `normalize_phone_path`. |
| 13 | dashboard.js:419,628,763 · history.js:82 · rules.js:93,100 · profiles.js:19 | Phone filenames injected into `innerHTML` unescaped. A file named `<img onerror=fetch('/api/run',…)>` executes same-origin; persisted into history.json and re-fires on every /history visit. `escapeHtml` exists at rules.js:464, unused. | `textContent` or `escapeHtml` everywhere; hoist to main.js. |
| 14 | device.py:127 | Serial regex requires uppercase `[A-Z0-9]+`; lowercase/hyphenated serials fall back to `usb_address`, which changes on reconnect and matches a **different phone** in the same port — running its move/sync rules. | Never match on port-derived id; require serial. |

## P1 — crashes / broken features

| # | Where | Problem | Fix |
|---|-------|---------|-----|
| 15 | runner.py:175,265 (RED) 183,195,262 (YELLOW) 196 (CYAN) | Uncommitted palette diff deleted these attrs; still referenced. AttributeError on "no rules with IDs", device-locked warning, manual-rule paths, unknown mode, and **every per-rule exception** (which then aborts remaining rules and hides the original error). | RED→ERROR, YELLOW→WARNING, CYAN→INFO. |
| 16 | runner.py:178-180 | `include_manual=True` runs **only** manual rules, contradicting docstring and web_ui.py:252 count. | Pick one semantic; fix name or filter. |
| 17 | web_ui.py:401 | `cfg.add_profile(config, profile_name=…, device_name=…, mtp_id=…)` — signature is `add_profile(config, profile: dict)`. TypeError → 500 on every web registration. Even fixed, no `id_type`/`id_value` written. | Route through `device.register_current_device`. |
| 18 | profiles.js:107 | POSTs `/api/profiles` and PUTs `/api/profiles/<name>` — routes don't exist (405). Add/Edit Profile always fails. | Add routes or point at `/api/device/register`. |
| 19 | rules.js:64,123,133 | `modeOrder` lacks `backup`; rules saved with `mode: "backup"` never render — invisible, undeletable from UI. | Add `backup`. |
| 20 | web_ui.py:218,231 | Check-then-set race: `running` set inside worker thread. Two POSTs → two concurrent runners over same MTP mount + shared DRY_RUN global. | Set `running=True` in handler under `threading.Lock`. |
| 21 | web_ui.py:241 | `sys.stdout = io.StringIO()` process-wide from a thread. With #20, stdout ends up pointing at a dead StringIO forever. Logs only harvested after run returns (no live progress); on exception restored without reading (all output lost). | Pass a writer into runner / use logging. |
| 22 | web_ui.py:350,356,362 | Smart-copy progress written to `current_run_status["stats"]` then overwritten at :362. Error regex `Errors:\s*\d+` never matches `⚠ Errors encountered  3` → failed runs show "Completed successfully". | Fix regex or, better, return structured stats from runner instead of parsing output. |
| 23 | dashboard.js:290,299,683 | `[DRY RUN MODE` bracket regex never matches new output (DRY RUN badge never renders — preview looks identical to real run). Op regex lacks `Backup`. Detail parser keys on emoji sentinels. | Structured JSON from runner; stop parsing ANSI text. |
| 24 | dashboard.js:794 | `isResultsExpanded`/`toggleResultsExpanded` undefined → ReferenceError on every Escape. | Delete or define. |
| 25 | main.py:174,208,221,233 | `pgrep -f 'python.*main.py.*--web'` kills any matching process on the box; SIGTERM then immediate bind → "Address already in use"; `--background` reports success even if child died. | PID file + cmdline check; poll for port free/up. |
| 26 | config.py:10 | `CONFIG_DIR = ~/Programming/project-cli/phone-migration` — hardcoded dev checkout. runner.py:123 tells user to inspect `~/.config/phone-migration/config.json`, which is never read. | XDG path; one-shot migration of old file. |
| 27 | config.py:85 + state.py:47 | Rule IDs unique per profile (`r-0001` everywhere) but resume state keyed by bare `rule_id` globally — two profiles share one backup state. | Key on `profile:rule_id`. |
| 28 | gio_utils.py:49 | No `timeout=` on any subprocess. Hung MTP blocks forever; web `running` stuck True, all later runs 409, no cancel. | Add timeouts + cancel/reset path. |
| 29 | operations.py:58,109,193,246,383,455 | `ensure_dir` not DRY_RUN-gated — preview creates real directory trees. | Guard. |

## P2 — correctness / robustness

- operations.py:92 etc.: both ternary branches identical → `//` on trailing slash.
- operations.py:120: entry that is neither dir nor regular falls through silently, no stats.
- operations.py:249: resume compares by existence only; truncated dest → skipped forever as "conflict".
- operations.py:323: `run_smart_copy_rule` discards `rename_duplicates`; runner.py:248 also hardcodes False, so web UI toggle ignored for backup.
- operations.py:408: `_cleanup_empty_dirs` deletes depth-1 subdirs with no emptiness check — relies on backend refusing.
- operations.py:538,543: bare `except:` (also browser.py:46).
- operations.py:649: `rename_duplicates=False` makes sync skip every changed file while delete_extraneous still runs.
- operations.py:679: delete_extraneous dir check omits `"2"` numeric type → never recurses on real MTP; mirroring one level deep.
- operations.py:690: name comparison desktop-vs-phone with no normalization (NFC/NFD, `~` backups) → delete/re-copy loop.
- runner.py:252: `backed_up += resumed` — re-runs report N files "backed up" with 0 transferred.
- runner.py:292: backup `skipped` = not copied (conflict) but summary says "Skipped (exist)".
- gio_utils.py:98: `gio_info` returns `{}` for both missing and failure.
- gio_utils.py:182,184: success line printed before run(); stderr discarded.
- paths.py:23: `Path("").resolve()` = CWD — empty desktop_path silently targets CWD (as sync source, mirrors CWD onto phone).
- paths.py:60,103: storage label prefix needs trailing slash; label not quoted.
- paths.py:132,155: `.bashrc` → ` (1).bashrc`; RuntimeError at 1000 dups aborts whole run.
- state.py:37,40,100,127: fixed tmp name (concurrent runs clobber); no fsync; O(n²) read-modify-write per file; failed list never pruned.
- config.py:33,65: non-atomic save; `config["profiles"]` KeyError on hand-edited file.
- device.py:33,52: one device per block (multi-volume drops siblings); `mtp://` anywhere in block = MTP.
- browser.py:46,57,74: bare `gio` on PATH; one `gio info` per entry (5k photos = 5k subprocesses in a request); `size.isdigit()` → 0.
- notifications.py:41,59: message starting with `-` parsed as option (add `--`); `except (…, Exception)`.
- web_ui.py:361,433,581,46: shallow dict of live `logs` list; `limit` unvalidated (`-1` drops newest); `debug=True` under `__main__`; history in `~/.config` while config is in dev path.
- main.py:339,353,367: no way to clear `manual_only` from CLI; `--run --manual` silently ignored (`include_manual` never passed); `--browse-phone` overloads `-v` to mean non-interactive.
- main.py:289-311,405: duplicated `--backup`/`--smart-copy` blocks; no deprecation warning; dead `return 0`.

## Tests

- 3 failures are test drift, not code bugs: `@patch('phone_migration.gio_info')` wrong target (→ `phone_migration.gio_utils.gio_info`); sync test asserts `errors>0` where code counts `skipped`; smart_copy mock omits `total_files` (state.py:134 should `.get`).
- test_operations.py:90,158: copy/move tests assert only `isinstance(stats, dict)` / key presence. Real result is `errors: 1` (mock never writes dest). Move's delete-after-verify loop has **zero** coverage.
- :206,234,242: `_delete_extraneous_on_phone` patched out in all sync tests — the phone-deletion path is untested.
- :275: state.py mocked everywhere; corrupt-JSON, atomic write, resume filtering untested.
- No coverage: paths.py, device.py, config.py, runner.py, web_ui.py, gio_utils.py (2 of 8 modules touched). Nothing imports runner or web_ui → #15 and #17 uncatchable. Cheap win: `test_imports()` + `app.test_client().post('/api/device/register')`.
- :14: sys.path hack; no pyproject/pytest.ini/conftest/CI. Root `test_colors.py` collected by pytest with zero assertions.

## Docs / repo hygiene

- README.md:46 "No external dependencies" — `--web` needs Flask + flask-cors (`requirements-web.txt`).
- README.md:110,160,407,418,440 · QUICKSTART.md:66,82 · warp.md:42,72,96: documented workflow is `--run` (dry-run only, never transfers) or `--run --dry-run` (flag does not exist, argparse exit 2). Needs `-y`.
- docs/RULE_MODES.md:30,65 `--no-rename` does not exist; :95,107,263 "smart comparison" overclaim (backup checks state file + name only); :104 per-rule rename config does not exist.
- README.md:536, QUICKSTART.md:129: `docs/TODO.md` deleted in 63d813c. README documents only move+sync; copy/backup/check/browse/manual/-y/notify/web flags undocumented.
- warp.md:111: example session fabricated/stale; shows deletions under bare `--run`.
- CHANGELOG.md: unmaintained since b9420d2; only `[Unreleased]`.
- test_colors.py: verbatim copy of runner.Colors (already drifted). Import instead; one assert (`set(re.findall(r'Colors\.([A-Z_]+)', runner_src)) <= set(vars(Colors))`) would have caught #15.
- DESIGN_SYSTEM.md:149,254 claims runner migration done — false. :23 SEPARATOR `#1A1D2E` vs code `#2A2E40`. Untracked at root; move to docs/ or drop.
- COLOR_REFERENCE.txt: contrast ratios wrong across the board (recomputed on #0D0E16: ERROR 9.7 not 7.8, INFO 10.9 not 6.5, …). "All colors meet WCAG AA" false — MUTED `#6E758C` = **4.2:1** (< 4.5 AA) and carries body text (runner.py:110,122,311). Duplicates DESIGN_SYSTEM.md; drop.
- Three Colors palettes ship in one CLI: runner.py (new), operations.py:13-22 + gio_utils.py + config.py:217,273 (legacy 16-color). Centralize in one module (repo rule).
- Emoji in CLI output: main.py:91-119,157,239,298,310,380,394,399 · runner.py:96,102,265,316 · config.py:250,313-333 · notifications.py titles · operations.py sentinels (which dashboard.js parses). Repo rule: no emoji; Nerd Font glyphs + fallback.
- `phone_migration/resources/`: untracked, unreferenced. `android-icon.webp` unused; `test/gamefree/**` = 283 three-byte `tone*.xml` files containing `mp3` — scratch data. Delete.

## Suggested order

1. Fix #15 (or `git stash` the palette diff) — currently every error path crashes.
2. #1, #2, #29 — make dry-run actually side-effect-free and non-sticky.
3. #10-#13 — remove CORS wildcard, confine paths, escape innerHTML.
4. #3-#9, #14 — data-loss guards in move/sync/backup, URI quoting, gio failure propagation.
5. Return structured stats from runner; delete ANSI/emoji parsing in web_ui.py + dashboard.js (kills #22, #23 and the drift class).
6. Tests: fix 3 drifted tests, add `test_imports`, real state fixture, one test each for move-delete and sync-delete_extraneous, `pyproject.toml` pytest config.
7. Docs: `-y` everywhere, drop `--dry-run`/`--no-rename`, XDG config path, delete warp.md/COLOR_REFERENCE.txt/resources/test.
